void setup() {
  Serial.begin(9600);         // USB 시리얼 모니터 (디버그 출력용, R4는 네이티브 USB)
  Serial1.begin(2400);        // 라즈베리파이 BAUD_RATE와 반드시 동일
  Serial.println(F("레이저 수신 대기 중..."));
}

void loop() {
  while (Serial1.available()) {
    char c = (char)Serial1.read();
    Serial.write(c);
  }
}